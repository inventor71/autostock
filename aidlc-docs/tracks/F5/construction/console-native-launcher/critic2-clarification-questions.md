# F5 critic 라운드2 후속 — 정책 분기 1개

엔지니어링 보강(#1 TTY exec / #2 콘솔 env 재현 / #3 EnvironmentFile 제거 / #4 worktree-서브모듈 / #6 naive-local)은
**이미 계획·설계에 반영**했습니다. 사용자 결정이 필요한 **정책 분기 1개**만 확인합니다.

## Question 1 — "사이드바 우선 시작" 구현 메커니즘 (FR-1/BR-7.1)
critic2가 코드로 입증: opencode home 화면은 중앙 컬럼이라 사이드바를 얹을 자리(`sidebar_content` consumer/side-region)가
없고, 그 슬롯은 **세션이 있어야** 렌더됩니다(`session/index.tsx:236` 게이팅). 따라서 두 가지 길이 있습니다.
(참고: 원래 Q1=A 답변이 "홈 건너뛰고 **바로 세션 뷰로** 들어가며 사이드바 표시"였으므로 B가 그 의도에 더 부합.)

A) **home 레이아웃 수술** — `home.tsx`를 가로(row)로 바꿔 우측에 autostock 패널을 마운트(로고/프롬프트 유지).
   홈 화면 자체를 트레이딩 대시보드처럼. (포크 home 레이아웃을 직접 고침 — blast radius 큼)

B) **세션 자동 진입** (추천) — 부팅 시 세션 라우트로 자동 이동(`-c` 최근 세션 또는 합성 빈 세션) →
   **기존에 동작하는 사이드바 경로를 그대로 재사용**. 켜자마자 세션 뷰 + autostock 사이드바.
   (원래 Q1=A 의도와 일치, 재사용이라 안전)

C) 둘 다 지원 / 토글 가능하게
X) 기타 (아래 [Answer]: 뒤에 설명)

[Answer]: B
