__pycache__/
*.py[cod]
*$py.class
*.so
*.egg-info/
dist/
build/
.eggs/
.turbo/

# Virtual environments
.venv/
venv/
env/

# IDE
.idea/
.vscode/
*.swp
*.swo

# Environment variables
.env
.env.local
# F10 verification: TEST paper account creds (keep .env.test.example tracked)
.env.test

# Data and models
data/*.csv
data/*.parquet
data/intraday/
models/*.pkl
models/*.pt
models/*.pth

# Logs
logs/
*.log

# Notebooks checkpoints
.ipynb_checkpoints/

# OS
.DS_Store
Thumbs.db

# SQLite
*.db
*.sqlite3

# Reports
reports/
*.html

# Agent journal (runtime state; CLAUDE.md template lives in src/agent/templates/)
workspace/

# F4 steering file-drop channel (runtime: commands/events/snapshot/token/processed).
# Anchored to repo root so it does NOT shadow src/agent/steering/ (tracked code).
/steering/

# F4 Unit B (operator-console, TS): node_modules anywhere; keep bun.lock (pinned).
node_modules/

# AI-DLC raw audit logs (local scratch; append-only, may contain verbatim prompts).
# Global timeline + per-track logs are local-only; per-track state.md IS tracked (committed record).
aidlc-docs/audit.md
aidlc-docs/tracks/*/audit.md
!aidlc-docs/tracks/_TEMPLATE/audit.md

# AI-DLC concurrent tracks: git worktrees live here — never commit worktree contents.
.claude/worktrees/
