"""Concrete HoldingsProvider implementations (F81).

Each module here owns one disclosure format's fragile bits (HTTP shape, parsing,
identifier mapping) so the rest of the holdings package stays source-agnostic.
"""
