"""Agentic trader subsystem: PM-agent brain (journal, tools, session, orchestrator).

Reuses the stable lower layers (core models, risk, execution, data) and adds the
decision + orchestration layer for an agentic, journaling trader. See
docs/DESIGN.md and the project memory for the architecture.
"""
